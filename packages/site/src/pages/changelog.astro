---
import ChangelogPage from "../views/ChangelogPage.astro";
import { loadChangelog, loadSiteCopy } from "../i18n/siteStrings.ts";

const copy = await loadSiteCopy("en");
const entries = await loadChangelog("en");
---
<ChangelogPage locale="en" copy={copy} entries={entries} />
