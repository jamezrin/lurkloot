name: Legacy release entry point (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This entry point is disabled. Use Prepare prerelease, Submit candidate, Cancel candidate, or rerun Promote reviewed release."
          exit 1
